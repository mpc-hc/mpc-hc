1) prevent multiple instances of the installer
2) use the defines from Version.h for MPC_COPYRIGHT_STR in VSFilter
3) define VSFilter's version numbers in a place accessible by
   the installer and VSFilter itself
4) build and statically link unrar library for out of the box support of rar'ed subtitles
   (or include unrar.dll in the packages)
